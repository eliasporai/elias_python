# Liberação de acesso a um laboratório
#VARIÁVEIS
nome_do_aluno = input("Digite o nome do aluno: ")
idade = int(input("digite a idade: "))
cadastro = input("Possui cadastro ativo? (sim/não)")

#regras 

if cadastro != "Sim" "sim" "S" "s":
    situacao = ("acesso permitido")
elif idade <14:
    situacao = ("acesso permitido somente com acompanhante")
else: 
    situacao = ("acesso permitido")